{
  "task_version": "1.0",
  "task_id": "invalid-secret-demo",
  "business_profile": "configs/example-business/business-profile.json",
  "objective": "This fixture must fail because a credential-like field is embedded in task inputs.",
  "mode": "research-only",
  "approved_domains": ["example.com"],
  "prohibited_actions": ["bypass-access-controls"],
  "inputs": {
    "api_key": "fake-value-for-negative-test"
  },
  "outputs": {
    "formats": ["json"],
    "destination": "artifacts/invalid/",
    "require_source_urls": true,
    "require_timestamps": true
  },
  "limits": {
    "max_items": 1,
    "max_retries": 0,
    "timeout_minutes": 1
  },
  "approval_policy": {
    "require_blueprint_confirmation": true,
    "require_test_approval": true,
    "consequential_actions": []
  },
  "validation": {
    "success_criteria": ["The validator rejects this fixture"],
    "duplicate_key": "source_url",
    "on_missing_data": "fail"
  }
}
